<main class="container">
  <h2>Rendered via MDEx</h2>
  <div class="prose">
    <% html =
      case @html do
        {:ok, content} when is_binary(content) -> content
        content when is_binary(content) -> content
        _ -> ""
      end %>
    {Phoenix.HTML.raw(html)}
  </div>
</main>
